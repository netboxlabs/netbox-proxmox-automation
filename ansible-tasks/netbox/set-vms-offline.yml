  - name: Collect decommissioned VM(s) to set as Offline in NetBox
    set_fact:
      nb_vm_offline: "{{ nb_vm_offline + [item] }}"
    loop: "{{ nb_vm_decomissioning }}"

#  - name: Update VM State to Active in NetBox
#    netbox.netbox.netbox_virtual_machine:
#      netbox_url: "{{ secrets_cfg.netbox.api_proto }}://{{ secrets_cfg.netbox.api_host }}:{{ secrets_cfg.netbox.api_port}}"
#      netbox_token: "{{ secrets_cfg.netbox.api_token }}"
#      data:
#        name: "{{ item['item'] }}"
#        status: "Active"
#      state: present
#    with_items: "{{ start_vms_job.results }}"

  - name: Update VM State to Offline in NetBox
    netbox.netbox.netbox_virtual_machine:
      netbox_url: "{{ secrets_cfg.netbox.api_proto }}://{{ secrets_cfg.netbox.api_host }}:{{ secrets_cfg.netbox.api_port}}"
      netbox_token: "{{ secrets_cfg.netbox.api_token }}"
      data:
        name: "{{ item }}"
        status: "Offline"
      state: present
    loop: "{{ nb_vm_offline }}"
