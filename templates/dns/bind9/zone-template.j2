;
; Auto generated -- DO NOT EDIT!
;
$ORIGIN {{ nb_dns_plugin_soa[item].origin }}.
$TTL {{ nb_dns_plugin_soa[item].ttl }}
@        IN        SOA        {{ nb_dns_plugin_soa[item].soa_ns_name }} {{ nb_dns_plugin_soa[item].soa_hostmaster }} (
                              {{ nb_dns_plugin_soa[item].serial }}   ; serial, today's date + today's serial #
                              {{ nb_dns_plugin_soa[item].refresh }}        ; refresh, seconds
                              {{ nb_dns_plugin_soa[item].retry }}         ; retry, seconds
                              {{ nb_dns_plugin_soa[item].expire }}      ; expire,seconds
                              {{ nb_dns_plugin_soa[item].minimum }} )      ; minimum TTL, seconds

;
                   {% for ns in nb_dns_plugin_ns[item]['ns'] %}
                   NS    {{ ns }}.
                   {% endfor %}

;

                   {% for mx in nb_dns_plugin_mx[item]['mx'] %}
                   MX    {{ mx }}.
                   {% endfor %}

;
; DNS entries
;
{% if item in nb_dns_plugin_rr %}
{% for dns_entry in nb_dns_plugin_rr[item]['rr'] %}
{% if dns_entry['rr_type'] != 'PTR' %}
{# {% if loop.index == 1 %}#}
{{ "{:<24} {:<10} {:<8} {}".format(dns_entry['rr_name'], dns_entry['rr_ttl'], dns_entry['rr_type'], dns_entry['rr_value']) }}
{#{% else %}
{{ "{:<24} {:<10} {:<8} {}".format('', dns_entry['rr_ttl'], dns_entry['rr_type'], dns_entry['rr_value']) }}
{% endif %}#}
{% else %}
{{ "{:<16} {:<4} {}".format(dns_entry['rr_name'], dns_entry['rr_type'], dns_entry['rr_value']) }}
{% endif %}
{% endfor %}
{% endif %}
;
; end DNS entries
;

